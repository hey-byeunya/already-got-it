# 버그 리포트 — 2026-07-23

- 카테고리 필터 칩·폼 입력창이 배경색과 거의 같아 경계가 안 보이던 문제 → `bg-input-bg` 토큰 신설로 해결
- 있템 목록 검색창+추가 버튼이 좁은 화면에서 줄바꿈되던 문제 → 검색창을 고정폭 대신 `flex-1 min-w-0`으로 변경해 해결
- 수정 화면 저장/삭제 버튼을 2:1 비율로 나누다 보니 창 폭이 1080px는 넘어야 내부 필드(사용기한/메모)가 나란히 붙던 버그 → 삭제 버튼을 고정폭으로 바꾸고 저장 버튼과 같은 줄에 배치해 해결
- 쓴템 목록의 "다 씀 날짜"가 마이그레이션 미적용 환경에서 항상 비어 보이던 문제 → `used_up_at` 대신 항상 존재하는 `updated_at`으로 표시하도록 변경
